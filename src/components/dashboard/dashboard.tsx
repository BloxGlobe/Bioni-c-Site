import DashboardLayout from '../../site-pages/dashboard/l@yout/DashboardLayout';

function Dashboard() {
  return (
    <main style={{ width: '100%', height: '100vh', margin: 0, padding: 0 }}>
      <DashboardLayout />
    </main>
  );
}

export default Dashboard;
